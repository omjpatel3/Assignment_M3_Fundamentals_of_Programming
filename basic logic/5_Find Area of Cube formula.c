#include<stdio.h>
main()
{
    int length,area;

    printf("\n\n\t Enter the length of side of cube:  ");
    scanf("%d",&length);

    area=6*length*length;
    printf("\n\n\t Area of the cube is %d",area);
}
